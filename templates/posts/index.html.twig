{% extends "base.html.twig" %}

{% block body %}
    <h1>Welcome to Bloggy !!!</h1>

    {% for post in posts %}
        <article>
            <h2>{{post.title}}</h2>
            <p>{{post.body}}</p>
        </article>
    {% endfor %}

{% endblock %}